# torch-yun Agent 指令

在本仓库工作时，修改代码、执行项目命令、回答项目开发问题之前，先读取项目开发手册。

Agent 入口文件：

- `torch-yun-vip/agents/AGENTS.md`

将 `torch-yun-vip/agents/AGENTS.md` 作为本仓库的权威 Agent 索引。除非用户明确给出新的冲突指令，否则需要遵循该文件引用的产品方向、架构设计、开发规范、格式化、测试和 Review 指令。
